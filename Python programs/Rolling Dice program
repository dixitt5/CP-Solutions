import random 
while True:     
     print("Press 1 if you want to roll a dice else just enter any key to exit from program")
     user = input("Press key: ")
     if user=="1":
        print("Value on dice is : "+random.randint(1,6))
     else:
        break
print("Program terminated '_'")
